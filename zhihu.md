# 我写了一个轻量级 JSON 压缩库，平均压缩率 52%，对大模型场景特别友好

## 起因

最近在做一个项目，后端返回的列表接口数据量很大，每个对象都带着完整的 key 名。比如一个 1000 条的用户列表，光 `"name"` 这个 key 就重复了 1000 次，`"email"`、`"phone"`、`"address"` 同理。

我算了一下，一个 1000 条的简单用户列表，原始 JSON 大约 148 KB，其中大部分体积被重复的 key 名吃掉了。

然后我就想：**能不能把 key 和数据分离，key 只存一份？**

于是就有了 **slimjson**。

## 一句话介绍

slimjson 是一个轻量级的 JavaScript 对象数组压缩工具。它把 `[{"name":"张三","age":28}, {"name":"李四","age":35}]` 这样的数组转换成 `{keys, rows}` 结构，消除重复 key，再通过序列化时省略 `null` 和引号来进一步压缩。

## 怎么用

### 安装

```bash
npm install slimjson
```

### 基础用法

```js
import { compress, decompress, stringify, parse } from 'slimjson';

const users = [
  { name: '张三', age: 28, city: '北京' },
  { name: '李四', age: 35, city: '上海' },
  { name: '王五', age: 22 },
];

const compressed = compress(users);
// {
//   keys: ['name', 'age', 'city'],
//   rows: [
//     ['张三', 28, '北京'],
//     ['李四', 35, '上海'],
//     ['王五', 22, null]    ← 缺失字段补 null
//   ]
// }

// 还原
const restored = decompress(compressed);
// 与原始数据完全一致
```

就这么简单。key 从每行重复变成只存一份，rows 里全是纯数据。

### 序列化时省略 null

光有 `{keys, rows}` 结构还不够，slimjson 还提供了 `stringify` 函数，序列化时把数组中的 `null` 省略为逗号空槽：

```js
stringify(compress(users));
// {keys:[name,age,city],rows:[[张三,28,北京],[李四,35,上海],[王五,22,]]}
//                                                                 ^^ null 省略为空槽，只保留逗号

// 对比 JSON.stringify
JSON.stringify(compress(users));
// {"keys":["name","age","city"],"rows":[["张三",28,"北京"],["李四",35,"上海"],["王五",22,null]]}
```

安全字符串还会自动省略引号（`"张三"` → `张三`），进一步减小体积。

### 解析时自动恢复

`parse` 能把省略的 null 全部恢复：

```js
const text = stringify(compress(users));
const parsed = parse(text);
// parsed 与 compress(users) 完全一致
```

## 嵌套对象也支持

实际项目中数据结构很少是扁平的。slimjson 完整支持嵌套对象和对象数组的递归压缩：

```js
const orders = [
  {
    orderId: 'A001',
    customer: '张三',
    items: [
      { name: '键盘', price: 299, specs: { color: '黑色', layout: '104键' } },
      { name: '鼠标', price: 99,  specs: { color: '白色', dpi: '4000' } },
    ]
  },
  {
    orderId: 'A002',
    customer: '李四',
    items: [
      { name: '显示器', price: 1999, specs: { color: '银色', size: '27寸' } },
    ]
  },
];

compress(orders);
// keys 中嵌套结构用对象表示：
// {
//   keys: ['orderId', 'customer', {
//     items: ['name', 'price', {
//       specs: ['color', 'layout', 'dpi', 'size']  ← specs 的 key 取所有订单的并集
//     }]
//   }],
//   rows: [
//     ['A001', '张三', [
//       ['键盘', 299, ['黑色', '104键', null, null]],  ← 缺失的 dpi、size 填 null
//       ['鼠标', 99,  ['白色', null, '4000', null]]
//     ]],
//     ['A002', '李四', [
//       ['显示器', 1999, ['银色', null, null, '27寸']]  ← 缺失的 layout、dpi 填 null
//     ]]
//   ]
// }
```

三层嵌套（订单 → 商品 → 规格）也能正确处理，key 自动取并集。

## 新功能：trimTrailingNulls

最近加了一个新选项 `trimTrailingNulls`，可以把每行尾部的 null 直接去掉：

```js
const data = [
  { name: '张三', age: 28, profile: { avatar: 'a.jpg', bio: 'Hello' } },
  { name: '李四', age: 35, profile: { avatar: 'b.jpg', file: null } },
  { name: '王五' },
];

// 默认（不 trim）
compress(data).rows;
// [
//   ['张三', 28, ['a.jpg', 'Hello', null]],
//   ['李四', 35, ['b.jpg', null, null]],
//   ['王五', null, null]
// ]

// 开启 trim
compress(data, { trimTrailingNulls: true }).rows;
// [
//   ['张三', 28, ['a.jpg', 'Hello']],   ← file 的 null 没了
//   ['李四', 35, ['b.jpg']],             ← bio、file 的 null 没了
//   ['王五']                              ← age、profile 的 null 没了
// ]
```

decompress 会自动把缺失的尾部值补回 null，roundtrip 完全一致。

这个功能在数据稀疏（不同对象字段差异大）的场景下效果最好。比如后端省略 null 字段返回的数据，trim 后能额外节省 0.5%~1% 的体积。

## 压缩效果实测

跑了一组基准测试（18 个场景），结果如下：

| 数据类型 | 数量 | 原始 | 压缩后 | 压缩率 |
|---------|------|------|--------|--------|
| 简单用户 | 10,000 | 1.45 MB | 882 KB | **40.7%** |
| 嵌套用户 | 1,000 | 236 KB | 153 KB | **35.0%** |
| 订单（每单1-5商品） | 500 | 167 KB | 72 KB | **56.7%** |
| 学校数据（6年级×4班×30生） | 24 | 215 KB | 89 KB | **58.8%** |
| 稀疏字段（500条×30字段） | 500 | 145 KB | 45 KB | **68.8%** |
| 稀疏字段（2000条×50字段） | 2,000 | 952 KB | 292 KB | **69.3%** |
| 深层嵌套（5层组织结构） | 5 | 635 KB | 289 KB | **54.5%** |

**规律很明显：**

1. 字段名越长、字段越多，压缩效果越好（key 占比高）
2. 对象数组（订单、学生列表）压缩效果显著
3. 稀疏字段压缩率最高 — 因为缺失字段的 null 被空槽省略了
4. trimTrailingNulls 在稀疏数据上额外节省 0.1%~0.2%

## 对大模型场景特别友好

这是我做 slimjson 之后发现的一个意外收获。把 slimjson 用在 LLM 相关场景，效果出奇地好：

### 场景一：压缩 prompt 中的结构化数据

比如你有一个数据库查询结果要喂给大模型做分析：

```js
// 原始 JSON：500 条用户数据，148 KB
const rawJSON = JSON.stringify(userList);

// slimjson 压缩后：87 KB，节省 41%
const compressed = stringify(compress(userList));
```

按 GPT-4 的定价，1000 token 约 $0.03，148 KB 的 JSON 大约 37000 token，压缩后大约 22000 token。**一次请求省 $0.45，一天 100 次就是 $45。**

### 场景二：压缩 function calling 返回结果

大模型工具调用的结果往往是结构化的对象数组。把这些结果压缩后再回传给模型，能显著减少上下文窗口的占用，让模型在有限 token 内处理更复杂的数据。

### 场景三：模型更容易理解

压缩后的 `{keys, rows}` 格式把 schema 和数据分离了。key 只出现一次，模型能更准确地理解数据结构，按字段名提取信息。相比重复 key 的原始 JSON，不容易产生混淆。

## 为什么不用 gzip？

好问题。gzip 确实压缩率更高，但有几个限制：

1. **gzip 是二进制**，不能直接当 JSON 用。slimjson 压缩后仍然是有效的文本，可以直接传输、存储、调试
2. **gzip 不可读**。slimjson 的 `{keys, rows}` 格式人类和模型都能直接读懂
3. **gzip 需要解压才能使用**。slimjson 的 `decompress` 直接返回可用的 JS 对象，不需要额外步骤
4. **slimjson 可以和 gzip 叠加使用**。先用 slimjson 去重 key，再用 gzip 压缩文本，效果更好

## 技术细节

如果你好奇内部实现，核心逻辑其实很简洁：

1. **buildKeys**：扫描所有对象，取 key 的并集（包括嵌套对象），按首次出现顺序排列
2. **buildRow**：按 keys 结构把每个对象转成行数组，缺失字段填 null
3. **trimTrailingNulls**：可选地去除行尾的 null（递归处理嵌套子行）
4. **stringify**：序列化时 null 变为空槽，安全字符串省略引号
5. **parse**：自写的解析器，能正确恢复省略的 null

没有依赖，核心代码只有 400 多行。测试覆盖率 100%（192 个用例）。

## 总结

| 特性 | 说明 |
|------|------|
| 去重 key | key 只存一份，rows 纯数据 |
| 嵌套支持 | 递归处理嵌套对象和对象数组 |
| null 省略 | 序列化时空槽代替 null 文字 |
| 引号省略 | 安全字符串自动省略引号 |
| trimTrailingNulls | 可选去除行尾 null |
| 零依赖 | 核心 400 行，开箱即用 |
| 100% 测试覆盖 | 192 个用例 |

GitHub 地址：https://github.com/LastHeaven/slimjson

欢迎 star、提 issue、提 PR。

## Benchmark：LLM 数据检索准确率

使用 209 个数据检索问题，测试不同输入格式对 LLM 理解能力的影响。

#### 数据集目录

| 数据集 | 行数 | 结构类型 | CSV 支持 | 可用率 |
| ------- | ---- | --------- | ----------- | ----------- |
| 统一的员工记录 | 100 | 统一 | ✓ | 100% |
| 带嵌套结构的电商订单 | 50 | 嵌套 | ✗ | 33% |
| 时间序列分析数据 | 60 | 统一 | ✓ | 100% |
| Top 100 GitHub 仓库 | 100 | 统一 | ✓ | 100% |
| 半统一事件日志 | 75 | 半统一 | ✗ | 50% |
| 深层嵌套配置 | 11 | 深层 | ✗ | 0% |
| 有效的完整数据集（对照组） | 20 | 统一 | ✓ | 100% |
| 数组被截断：末尾删除 3 行 | 17 | 统一 | ✓ | 100% |
| 在声明长度之外添加了多余行 | 23 | 统一 | ✓ | 100% |
| 字段数量不一致（第 10 行缺少 salary） | 20 | 统一 | ✓ | 100% |
| 缺少必填字段（多行缺少 email） | 20 | 统一 | ✓ | 100% |

**结构类型：**
- **统一**：所有对象具有相同的字段和基本值
- **半统一**：混合了统一和非统一结构
- **嵌套**：包含嵌套结构的对象（嵌套对象或数组）
- **深层**：高度嵌套，几乎没有表格化可能

**CSV 支持：** ✓（支持），✗（不支持——需要有损扁平化）

**可用率：** 符合 TOON 表格格式（包含基本值的统一对象）的数组百分比

#### 效率排名（每千 Token 的准确率）

按效率（每 1000 token 的准确率百分比）排名：

```
slimjson       ████████████████████   44.4 准确率%/千token  │  94.7% 准确率  │  2,134 tokens
TOON           ███████████████░░░░░   34.0 准确率%/千token  │  92.8% 准确率  │  2,734 tokens
JSON compact   ██████████████░░░░░░   31.0 准确率%/千token  │  95.2% 准确率  │  3,072 tokens
YAML           ███████████░░░░░░░░░   25.4 准确率%/千token  │  94.3% 准确率  │  3,716 tokens
JSON           ██████████░░░░░░░░░░   21.1 准确率%/千token  │  95.7% 准确率  │  4,538 tokens
XML            ████████░░░░░░░░░░░░   18.5 准确率%/千token  │  95.7% 准确率  │  5,162 tokens
```

*效率分数 =（准确率% ÷ Token 数）× 1,000。越高越好。*

> slimjson 达到 **94.7%** 准确率（对比 JSON 的 95.7%），同时仅使用了 **53.0% 更少的 token**。

**关于 CSV 的说明：** 排名中排除了 CSV，因为它仅支持 209 个问题中的 109 个（仅限扁平表格数据）。虽然 CSV 在简单表格数据上 token 效率很高，但它无法表示其他格式能处理的嵌套结构。

#### 单模型准确率

1 个 LLM 在 209 个数据检索问题上的准确率：

```
deepseek-v4-flash
  JSON           ███████████████████░    95.7% (200/209)
  XML            ███████████████████░    95.7% (200/209)
  JSON compact   ███████████████████░    95.2% (199/209)
→ slimjson       ███████████████████░    94.7% (198/209)
  YAML           ███████████████████░    94.3% (197/209)
  TOON           ███████████████████░    92.8% (194/209)
  CSV            ██████████████████░░    91.7% (100/109)
```

> slimjson 达到 **94.7% 准确率**（对比 JSON 的 95.7%），在这些数据集上仅使用了 **53.0% 更少的 token**。

#### 按题型的表现

| 题型 | JSON | XML | JSON compact | slimjson | YAML | TOON | CSV |
| ------------- | ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| 字段检索 | 98.5% | 97.1% | 98.5% | 95.6% | 97.1% | 91.2% | 96.9% |
| 聚合计算 | 98.4% | 96.8% | 95.2% | 95.2% | 93.7% | 95.2% | 86.2% |
| 条件筛选 | 97.9% | 97.9% | 100.0% | 100.0% | 100.0% | 100.0% | 96.3% |
| 结构感知 | 88.0% | 92.0% | 84.0% | 92.0% | 88.0% | 88.0% | 87.5% |
| 结构验证 | 40.0% | 60.0% | 60.0% | 40.0% | 40.0% | 40.0% | 80.0% |

#### 按数据集的表现

##### 统一的员工记录

| 格式 | 准确率 | Token | 正确/总计 |
| ------ | -------- | ------ | ------------- |
| `slimjson` | 100.0% | 2,354 | 41/41 |
| `csv` | 95.1% | 2,336 | 39/41 |
| `toon` | 97.6% | 2,492 | 40/41 |
| `json-compact` | 97.6% | 3,919 | 40/41 |
| `yaml` | 100.0% | 4,982 | 41/41 |
| `json-pretty` | 97.6% | 6,326 | 40/41 |
| `xml` | 100.0% | 7,286 | 41/41 |

##### 带嵌套结构的电商订单

| 格式 | 准确率 | Token | 正确/总计 |
| ------ | -------- | ------ | ------------- |
| `slimjson` | 100.0% | 4,616 | 41/41 |
| `json-compact` | 97.6% | 6,875 | 40/41 |
| `toon` | 95.1% | 7,299 | 39/41 |
| `yaml` | 95.1% | 8,456 | 39/41 |
| `json-pretty` | 95.1% | 10,842 | 39/41 |
| `xml` | 95.1% | 12,180 | 39/41 |

##### 时间序列分析数据

| 格式 | 准确率 | Token | 正确/总计 |
| ------ | -------- | ------ | ------------- |
| `slimjson` | 100.0% | 1,487 | 30/30 |
| `csv` | 96.7% | 1,408 | 29/30 |
| `toon` | 93.3% | 1,550 | 28/30 |
| `json-compact` | 100.0% | 2,351 | 30/30 |
| `yaml` | 96.7% | 2,951 | 29/30 |
| `json-pretty` | 100.0% | 3,678 | 30/30 |
| `xml` | 100.0% | 4,386 | 30/30 |

##### Top 100 GitHub 仓库

| 格式 | 准确率 | Token | 正确/总计 |
| ------ | -------- | ------ | ------------- |
| `toon` | 87.9% | 8,779 | 29/33 |
| `csv` | 84.8% | 8,527 | 28/33 |
| `slimjson` | 81.8% | 8,574 | 27/33 |
| `json-compact` | 90.9% | 11,464 | 30/33 |
| `yaml` | 90.9% | 13,141 | 30/33 |
| `json-pretty` | 90.9% | 15,157 | 30/33 |
| `xml` | 87.9% | 17,105 | 29/33 |

##### 半统一事件日志

| 格式 | 准确率 | Token | 正确/总计 |
| ------ | -------- | ------ | ------------- |
| `slimjson` | 93.3% | 3,466 | 28/30 |
| `json-compact` | 90.0% | 4,793 | 27/30 |
| `yaml` | 93.3% | 5,798 | 28/30 |
| `json-pretty` | 100.0% | 6,759 | 30/30 |
| `toon` | 90.0% | 5,769 | 27/30 |
| `xml` | 96.7% | 7,668 | 29/30 |

##### 深层嵌套配置

| 格式 | 准确率 | Token | 正确/总计 |
| ------ | -------- | ------ | ------------- |
| `json-compact` | 100.0% | 562 | 29/29 |
| `slimjson` | 100.0% | 571 | 29/29 |
| `toon` | 100.0% | 653 | 29/29 |
| `yaml` | 96.6% | 675 | 28/29 |
| `json-pretty` | 100.0% | 918 | 29/29 |
| `xml` | 100.0% | 1,007 | 29/29 |

#### 测试内容说明

本基准测试评估不同输入格式下 **LLM 的理解和数据检索准确率**。每个 LLM 接收格式化数据并回答相关问题。本测试**不**测试模型生成输出格式的能力——仅测试读取和理解能力。

#### 测试数据集

共 11 个数据集，用于测试不同结构模式和验证能力：

**主要数据集：**

1. **表格型**（100 条员工记录）：字段一致的统一对象——TOON 表格格式最优。
2. **嵌套型**（50 个电商订单）：含嵌套客户对象和商品数组的复杂结构。
3. **分析型**（60 天指标数据）：含日期和数值的时间序列数据。
4. **GitHub 型**（100 个仓库）：来自 GitHub Star 数最多的仓库的真实数据。
5. **事件日志型**（75 条日志）：约 50% 扁平日志、约 50% 含嵌套错误对象的半统一数据。
6. **嵌套配置型**（1 个配置）：高度嵌套的配置，几乎没有表格化可能。

**结构验证数据集：**

7. **对照组**：有效的完整数据集（验证基线）
8. **截断**：数组末尾删除 3 行（测试 `[N]` 长度检测）
9. **多余行**：在声明长度之外添加了 3 行
10. **宽度不匹配**：字段数量不一致（第 10 行缺少 salary）
11. **缺少字段**：系统性字段缺失（多行缺少 email）

#### 题型

动态生成 209 个问题，覆盖五类：

- **字段检索（33%）**：直接值查找或可直接从记录中读取的值（包括布尔值和简单计数如数组长度）
  - 示例："Alice 的工资是多少？" → `75000`
  - 示例："订单 ORD-0042 中有多少商品？" → `3`
  - 示例："订单 ORD-0042 的客户名称是什么？" → `John Doe`

- **聚合计算（30%）**：数据集级别的总计和平均值，以及单条件过滤（计数、求和、最小/最大比较）
  - 示例："有多少员工在工程部门？" → `17`
  - 示例："所有订单的总收入是多少？" → `45123.50`
  -示例："有多少员工工资 > 80000？" → `23`

- **条件筛选（23%）**：需要复合逻辑的多条件查询（跨字段的 AND 约束）
  - 示例："销售部门有多少人工资 > 80000？" → `5`
  - 示例："有多少在职员工工作年限超过 10 年？" → `8`

- **结构感知（12%）**：测试格式原生的结构能力（TOON 的 `[N]` 计数和 `{fields}`，CSV 的标题行）
  - 示例："数据集中有多少员工？" → `100`
  - 示例："列出员工的字段名" → `id, name, email, department, salary, yearsExperience, active`
  - 示例："最后一个员工的部门是什么？" → `Sales`

- **结构验证（2%）**：测试通过结构元数据检测不完整、截断或损坏数据的能力
  - 示例："数据是否完整有效？" → `YES`（对照组）或 `NO`（损坏数据集）
  - 测试 TOON 的 `[N]` 长度验证和 `{fields}` 一致性检查
  - 展示 CSV 缺乏结构验证能力

#### 评估流程

1. **格式转换**：每个数据集转换为所有 7 种格式（JSON、XML、JSON compact、slimjson、YAML、TOON、CSV）。
2. **查询 LLM**：每个模型接收格式化数据 + 提示中的问题，提取答案。
3. **确定性验证**：使用类型感知比较验证答案（例如 `50000` = `$50,000`，`Engineering` = `engineering`，`2025-01-01` = `January 1, 2025`），无需 LLM 评审。

#### 模型与配置

- **测试模型**：`deepseek-v4-flash`
- **Token 计数**：使用 `gpt-tokenizer` 的 `o200k_base` 编码（GPT-5 分词器）
- **Temperature**：未设置（使用模型默认值）
- **总评估次数**：209 题 × 7 种格式 × 1 个模型 = 1,463 次 LLM 调用

## Benchmark：Token 效率对比

#### 混合结构赛道

包含嵌套或半统一结构的数据集。CSV 被排除，因为它无法正确表示这些结构。

```
🛒 电商订单（含嵌套结构）  ┊  表格化：33%
   │
   slimjson            ████████░░░░░░░░░░░░    46,233 tokens
   ├─ 对比 JSON          (−57.8%)               109,574 tokens
   ├─ 对比 JSON compact  (−33.5%)                69,528 tokens
   ├─ 对比 TOON          (−36.9%)                73,246 tokens
   ├─ 对比 YAML          (−45.9%)                85,451 tokens
   └─ 对比 XML           (−62.5%)               123,272 tokens

📃 半统一事件日志  ┊  表格化：50%
   │
   slimjson            ██████████░░░░░░░░░░    91,630 tokens
   ├─ 对比 JSON          (−49.4%)               181,141 tokens
   ├─ 对比 JSON compact  (−28.7%)               128,480 tokens
   ├─ 对比 TOON          (−40.5%)               154,032 tokens
   ├─ 对比 YAML          (−41.0%)               155,346 tokens
   └─ 对比 XML           (−55.5%)               205,796 tokens

🧩 深层嵌套配置  ┊  表格化：0%
   │
   slimjson            ████████████░░░░░░░░       547 tokens
   ├─ 对比 JSON          (−39.6%)                   905 tokens
   ├─ 对比 JSON compact  (−0.9%)                    552 tokens
   ├─ 对比 TOON          (−11.5%)                   618 tokens
   ├─ 对比 YAML          (−17.4%)                   662 tokens
   └─ 对比 XML           (−45.1%)                   997 tokens

──────────────────────────────────── 总计 ────────────────────────────────────
   slimjson            █████████░░░░░░░░░░░   138,410 tokens
   ├─ 对比 JSON          (−52.5%)               291,620 tokens
   ├─ 对比 JSON compact  (−30.3%)               198,560 tokens
   ├─ 对比 TOON          (−39.3%)               227,896 tokens
   ├─ 对比 YAML          (−42.7%)               241,459 tokens
   └─ 对比 XML           (−58.1%)               330,065 tokens
```

#### 纯扁平赛道

包含扁平表格结构的数据集，CSV 可用。

```
👥 统一的员工记录  ┊  表格化：100%
   │
   CSV                 ████████████████████    47,137 tokens
   slimjson            ████████████████████    47,067 tokens   (对比 CSV −0.1%)
   ├─ 对比 JSON          (−63.0%)               127,050 tokens
   ├─ 对比 JSON compact  (−40.5%)                79,046 tokens
   ├─ 对比 TOON          (−5.8%)                 49,966 tokens
   ├─ 对比 YAML          (−52.9%)               100,033 tokens
   └─ 对比 XML           (−67.9%)               146,596 tokens

📈 时间序列分析数据  ┊  表格化：100%
   │
   CSV                 ███████████████████░     8,392 tokens
   slimjson            ████████████████████     8,767 tokens   (对比 CSV +4.5%)
   ├─ 对比 JSON          (−60.6%)                22,254 tokens
   ├─ 对比 JSON compact  (−38.3%)                14,220 tokens
   ├─ 对比 TOON          (−3.9%)                  9,124 tokens
   ├─ 对比 YAML          (−50.9%)                17,867 tokens
   └─ 对比 XML           (−67.1%)                26,625 tokens

⭐ Top 100 GitHub 仓库  ┊  表格化：100%
   │
   CSV                 ████████████████████     8,512 tokens
   slimjson            ████████████████████     8,550 tokens   (对比 CSV +0.4%)
   ├─ 对比 JSON          (−43.5%)                15,144 tokens
   ├─ 对比 JSON compact  (−25.4%)                11,454 tokens
   ├─ 对比 TOON          (−2.2%)                  8,744 tokens
   ├─ 对比 YAML          (−34.9%)                13,128 tokens
   └─ 对比 XML           (−50.0%)                17,095 tokens

──────────────────────────────────── 总计 ────────────────────────────────────
   CSV                 ████████████████████    64,041 tokens
   slimjson            ████████████████████    64,384 tokens   (对比 CSV +0.5%)
   ├─ 对比 JSON          (−60.8%)               164,448 tokens
   ├─ 对比 JSON compact  (−38.5%)               104,720 tokens
   ├─ 对比 TOON          (−5.1%)                 67,834 tokens
   ├─ 对比 YAML          (−50.9%)               131,028 tokens
   └─ 对比 XML           (−66.2%)               190,316 tokens
```
